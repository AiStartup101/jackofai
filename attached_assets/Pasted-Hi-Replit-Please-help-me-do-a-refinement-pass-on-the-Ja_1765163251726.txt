Hi Replit,

Please help me do a refinement pass on the JackofAI site (Next.js + app router + Tailwind).
Don’t rewrite everything – just apply the specific updates below.

────────────────────────────────
1) HOMEPAGE STRUCTURE (app/page.tsx)
────────────────────────────────

A. SECTION IDS FOR ANCHORS

In `app/page.tsx`:

1. On the “Our upcoming apps” section wrapper, add:
   id="products"

Example (adjust to the actual container element):
<section id="products" className="mt-16">...</section>

2. On the “Work with us: talent & collaborators” section wrapper, add:
   id="talent"

3. On the “Contact” section wrapper, add:
   id="contact"


B. HERO – SMALL CLARITY IMPROVEMENT

Find the main hero section heading + text (the area that says “We build warm, simple AI apps that bring people closer”).

Under the main hero paragraph, add one short line:

<p className="text-slate-300 mt-3 text-sm sm:text-base">
  We focus on small, calm products that support reflection, celebration, and everyday connection – not more noise.
</p>

Keep the existing buttons as they are (Explore ChristmasPulse, Explore FamilyPulse, Join Early Access, Work With Us, etc).


C. NEWSLETTER SIGNUP (IF NOT ALREADY WIRED)

If there is already a `NewsletterSignup` component in `app/components/NewsletterSignup.tsx`, leave it.
If not, create it with this code:

"use client";

import { useState, FormEvent } from "react";

export default function NewsletterSignup() {
  const [email, setEmail] = useState("");
  const [status, setStatus] = useState<"idle" | "submitting" | "success" | "error">("idle");

  const handleSubmit = (e: FormEvent) => {
    e.preventDefault();
    if (!email) return;
    setStatus("submitting");
    setTimeout(() => {
      setStatus("success");
      setEmail("");
    }, 600);
  };

  return (
    <div className="mt-10 rounded-2xl border border-white/10 bg-white/5 px-5 py-5 max-w-xl">
      <h3 className="text-lg font-semibold text-white">
        Get updates & early access
      </h3>
      <p className="text-sm text-slate-300 mt-2">
        Be the first to hear about new apps, early access invites, and collaboration opportunities.
      </p>
      <form onSubmit={handleSubmit} className="mt-4 flex flex-col gap-3 sm:flex-row">
        <input
          type="email"
          required
          placeholder="Your email address"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          className="flex-1 rounded-xl bg-slate-900/70 border border-white/10 px-3 py-2 text-sm text-slate-100 placeholder:text-slate-500 focus:outline-none focus:ring-2 focus:ring-sky-400"
        />
        <button
          type="submit"
          disabled={status === "submitting"}
          className="inline-flex items-center justify-center rounded-xl bg-sky-500 px-4 py-2 text-sm font-medium text-white hover:bg-sky-400 disabled:opacity-60"
        >
          {status === "submitting" ? "Sending..." : "Get updates"}
        </button>
      </form>
      {status === "success" && (
        <p className="mt-2 text-xs text-emerald-300">
          Thank you — you’re on the list.
        </p>
      )}
      {status === "error" && (
        <p className="mt-2 text-xs text-rose-300">
          Something went wrong. Please try again.
        </p>
      )}
    </div>
  );
}

Then in `app/page.tsx`:

import NewsletterSignup from "./components/NewsletterSignup";

Place `<NewsletterSignup />` once under the hero section (for example just before the “Our upcoming apps” block).


D. LIVE “CURRENT PRIORITY NEEDS” STRIP ON HOMEPAGE

At the top of `app/page.tsx` (inside the component, above `return`), add or update:

const currentNeeds = [
  "React Native developer (short sprint)",
  "Next.js frontend collaborator",
  "UI/UX designer for calm mobile apps",
  "Instagram & Reels collaborator",
  "YouTube shorts / video editor",
  "IP / patent advisor (apps & AI)",
  "Early adopters for ChristmasPulse & FamilyPulse testing"
];

In the “Work with us: talent & collaborators” section, directly under the intro paragraph and BEFORE the four cards grid, insert:

<div className="mt-4 rounded-2xl bg-white/5 border border-white/10 px-4 py-3">
  <div className="flex items-center gap-2 text-xs font-medium text-slate-200 uppercase tracking-wide">
    <span className="inline-flex h-2 w-2 rounded-full bg-emerald-400 animate-pulse" />
    <span>Current priority needs</span>
  </div>
  <div className="mt-3 flex flex-wrap gap-2 text-xs text-slate-200">
    {currentNeeds.map((item) => (
      <span
        key={item}
        className="rounded-full border border-white/15 bg-white/5 px-3 py-1 backdrop-blur-sm hover:border-sky-400 hover:text-sky-200 transition"
      >
        {item}
      </span>
    ))}
  </div>
</div>

Leave the four cards (builders, experts, investors, early adopters) and the “Share your details” block as they currently are.


E. CONTACT SECTION – KEEP BUT ENSURE SOCIAL LINKS

In the Contact block on the homepage (id="contact"), ensure it contains:

- Email (hello@jackofai.com)  
- Phone (+61 426 996 009)  
- WhatsApp note  
- Social “Find us” pills: LinkedIn, Instagram, YouTube, GitHub  

The structure you already have is fine; just keep them as a row of rounded buttons.


────────────────────────────────
2) OPPORTUNITIES PAGE (app/opportunities/page.tsx)
────────────────────────────────

We want this to feel like a clean explanation page with its own “Current needs” snapshot.

Replace the content of `app/opportunities/page.tsx` with:

export default function OpportunitiesPage() {
  return (
    <main className="max-w-3xl mx-auto py-20 px-4 text-slate-100 space-y-8">
      <header className="space-y-3">
        <h1 className="text-3xl font-bold text-white">Opportunities at JackofAI</h1>
        <p className="text-slate-300">
          We’re not a traditional hiring pipeline. We’re building a small network of people who enjoy crafting 
          thoughtful digital products – and we collaborate in flexible, project-based ways.
        </p>
      </header>

      <section className="rounded-2xl border border-white/10 bg-white/5 px-4 py-4">
        <div className="flex items-center gap-2 text-xs font-medium uppercase tracking-wide text-slate-200">
          <span className="inline-flex h-2 w-2 rounded-full bg-emerald-400 animate-pulse" />
          <span>Current priority needs</span>
        </div>
        <ul className="mt-3 text-sm text-slate-200 space-y-1">
          <li>• React Native developer for short sprints and prototypes</li>
          <li>• Next.js frontend collaborator for landing pages & micro-sites</li>
          <li>• UI/UX designer focused on calm, modern mobile apps</li>
          <li>• Social media / content partner for LinkedIn, Instagram & YouTube</li>
          <li>• IP advisor familiar with software & AI-related patents</li>
        </ul>
        <p className="mt-2 text-[11px] text-slate-400">
          These needs change over time – even if the list doesn’t match you perfectly, we’re still happy to hear from you.
        </p>
      </section>

      <section className="space-y-3">
        <h2 className="text-2xl font-semibold text-white">For builders & creators</h2>
        <p className="text-slate-300">
          Ideal if you like turning ideas into working products and experiences:
        </p>
        <ul className="list-disc pl-6 space-y-2 text-slate-200 text-sm">
          <li>App developers (React Native, Next.js, full-stack).</li>
          <li>UI/UX designers for modern, calm mobile interfaces.</li>
          <li>Motion designers, illustrators, and video creators.</li>
          <li>Social media creators and marketing partners.</li>
        </ul>
      </section>

      <section className="space-y-3">
        <h2 className="text-2xl font-semibold text-white">For experts & advisors</h2>
        <p className="text-slate-300">
          We welcome conversations with people experienced in consumer technology, AI-assisted products, 
          digital wellbeing, behaviour design, and intellectual property for software and apps.
        </p>
      </section>

      <section className="space-y-3">
        <h2 className="text-2xl font-semibold text-white">For investors & strategic partners</h2>
        <p className="text-slate-300">
          We’re open to thoughtful early conversations with angels, operators, and partners who understand 
          calm consumer apps and brand-led product building.
        </p>
      </section>

      <section className="space-y-3">
        <h2 className="text-2xl font-semibold text-white">How to reach us</h2>
        <p className="text-slate-300">
          If you’d like to explore working together, please email a short note with:
        </p>
        <ul className="list-disc pl-6 space-y-2 text-slate-200 text-sm">
          <li>Your name and what you do.</li>
          <li>A short bio and any relevant experience.</li>
          <li>Links to your work (GitHub, portfolio, Dribbble, Instagram, YouTube, LinkedIn, etc.).</li>
          <li>The type of collaboration you’re interested in.</li>
        </ul>
        <p>
          Email:{" "}
          <a href="mailto:hello@jackofai.com" className="text-sky-300">
            hello@jackofai.com
          </a>
        </p>
      </section>
    </main>
  );
}

────────────────────────────────
3) EARLY ACCESS PAGE (app/early-access/page.tsx)
────────────────────────────────

Tighten copy slightly and keep structure.

Replace `app/early-access/page.tsx` with:

export default function EarlyAccessPage() {
  return (
    <main className="max-w-3xl mx-auto py-20 px-4 text-slate-100 space-y-8">
      <header className="space-y-3">
        <h1 className="text-3xl font-bold text-white">Early access program</h1>
        <p className="text-slate-300">
          We believe the best products are built with real people, not in isolation. 
          Our early access program invites a small group of testers and supporters to help shape 
          ChristmasPulse and FamilyPulse as they evolve.
        </p>
      </header>

      <section className="space-y-3">
        <h2 className="text-2xl font-semibold text-white">Who it’s for</h2>
        <ul className="list-disc pl-6 space-y-2 text-slate-200 text-sm">
          <li>People who enjoy trying early versions of apps.</li>
          <li>Families and individuals who value emotional connection and meaningful moments.</li>
          <li>Creators and storytellers who want to experiment with new tools.</li>
        </ul>
      </section>

      <section className="space-y-3">
        <h2 className="text-2xl font-semibold text-white">What you’ll get</h2>
        <ul className="list-disc pl-6 space-y-2 text-slate-200 text-sm">
          <li>Early access to features before public release.</li>
          <li>Occasional behind-the-scenes updates from the founder.</li>
          <li>An opportunity to influence how the apps feel and work.</li>
        </ul>
      </section>

      <section className="space-y-3">
        <h2 className="text-2xl font-semibold text-white">What we ask in return</h2>
        <ul className="list-disc pl-6 space-y-2 text-slate-200 text-sm">
          <li>Honest, kind feedback on what feels useful or confusing.</li>
          <li>Consent before sharing any stories or screenshots publicly.</li>
          <li>Patience – early builds may be rough around the edges.</li>
        </ul>
      </section>

      <section className="space-y-3">
        <h2 className="text-2xl font-semibold text-white">How to join</h2>
        <p className="text-slate-300">
          If you’d like to be considered for early access, please send a short note to{" "}
          <a href="mailto:hello@jackofai.com" className="text-sky-300">hello@jackofai.com</a> with:
        </p>
        <ul className="list-disc pl-6 space-y-2 text-slate-200 text-sm">
          <li>Who you are and where you’re based.</li>
          <li>Whether you’re more interested in ChristmasPulse, FamilyPulse, or both.</li>
          <li>Why early access appeals to you.</li>
        </ul>
      </section>
    </main>
  );
}

────────────────────────────────
4) ABOUT, FOUNDER, PRESS PAGES
────────────────────────────────

These pages already look good based on the content you generated earlier.
No structural changes needed right now – you can keep them as they are for this pass.


────────────────────────────────
5) FOOTER – ENSURE LINKS
────────────────────────────────

In the footer, please ensure the following links exist (they already appear to be there, just keep them):

- About → "/about"
- Opportunities → "/opportunities"
- Early Access → "/early-access"
- Founder → "/founder"
- Press → "/press"
- Privacy Policy
- Terms & Conditions
- Cookie Notice
- AI Disclaimer

Do not change the company line:
"JackofAI™ Pty Ltd. Warm, simple AI products that bring people closer."


────────────────────────────────

After applying all these changes, please save, build, and redeploy the site.

Thank you!